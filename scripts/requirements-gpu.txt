# GPU training requirements — use Unsloth's official install
# On Lambda.ai / RunPod with A100/H100:
#   pip install "unsloth[cu124-ampere] @ git+https://github.com/unslothai/unsloth.git"
#   pip install -r scripts/requirements-gpu.txt
#
# The unsloth install above handles torch, transformers, peft, trl, bitsandbytes
# These are the additional deps only:
pyyaml>=6.0
datasets>=2.20
